# 🔍 Checkstyle Hub

**Checkstyle Hub** — це сучасний вебзастосунок для автоматичного аналізу якості Java-коду за допомогою інструменту
Checkstyle. Система дозволяє аналізувати публічні GitHub репозиторії, отримувати результати в реальному часі та гнучко
налаштовувати правила перевірки коду.

---

## 📋 Зміст

- [Про проект](#-про-проект)
- [Основні можливості](#-основні-можливості)
- [Технології](#-технології)
- [Архітектура](#-архітектура)
- [Встановлення та запуск](#-встановлення-та-запуск)
- [Використання](#-використання)
- [Структура проєкт](#-структура-проекту)
- [Конфігурація](#-конфігурація)
- [API](#-api)

---

## 🎯 Про проект

Checkstyle Hub — це фронтенд-частина системи аналізу Java-коду, яка інтегрується з backend-сервером для виконання
перевірок Checkstyle. Застосунок побудований з використанням React та TypeScript, забезпечуючи сучасний та інтуїтивно
зрозумілий інтерфейс користувача.

### Призначення

- **Для розробників**: швидка перевірка якості коду перед комітом
- **Для команд**: централізована система контролю стандартів кодування
- **Для навчання**: вивчення кращих практик Java-розробки

---

## ✨ Основні можливості

### 🚀 Аналіз репозиторіїв

- Підтримка публічних GitHub репозиторіїв
- Автоматичне клонування та аналіз Java-файлів
- Показ прогресу в реальному часі через WebSocket

### 📊 Візуалізація результатів

- Детальна таблиця знайдених порушень
- Інформація про файл, рядок та тип порушення
- Фільтрація за рівнем критичності (ERROR, WARNING, INFO)
- Підрахунок загальної кількості проблем

### 🎛️ Гнучкі налаштування

- 20+ правил Checkstyle для увімкнення/вимкнення
- Категоризовані правила (Імпорти, Блоки, Код, Дизайн)
- Налаштування суворості перевірок
- Конфігурація довжини рядка коду
- Швидке увімкнення/вимкнення всіх правил одразу

### 📡 Реальний час

- WebSocket-з'єднання для логів
- Автоматичний polling статусу аналізу (кожні 2 секунди)
- Миттєве оновлення статусу аналізу
- Відображення кроків виконання процесу (PENDING → CLONING → ANALYZING → COMPLETED)

### 🎨 Сучасний UI/UX

- Темна тема з градієнтами та blur-ефектами
- Адаптивний дизайн для різних розмірів екранів
- Анімації та плавні переходи
- Інтуїтивна навігація

---

## 🛠 Технології

### Frontend Stack

| Технологія       | Версія  | Призначення                   |
|------------------|---------|-------------------------------|
| **React**        | 19.1.1  | Бібліотека для побудови UI    |
| **TypeScript**   | 5.9.3   | Типізація та безпека коду     |
| **Vite**         | 7.1.7   | Швидкий bundler та dev-сервер |
| **Tailwind CSS** | 4.1.17  | Utility-first CSS фреймворк   |
| **Lucide React** | 0.552.0 | Іконки                        |

### Комунікація

- **@stomp/stompjs** — WebSocket через STOMP протокол
- **sockjs-client** — Fallback для WebSocket
- **Fetch API** — REST API запити

### Developer Experience

- **ESLint** — Лінтинг коду
- **PostCSS & Autoprefixer** — CSS обробка
- **react-hot-toast** — Сповіщення користувача
- **Vitest** — Unit тестування
- **Testing Library** — Тестування React компонентів

---

## 🏗 Архітектура

### Компонентна структура

```
src/
├── components/          # React компоненти
│   ├── AnalysisForm.tsx       # Форма для введення URL репозиторія
│   ├── ConfigurationModal.tsx # Модальне вікно налаштувань
│   ├── LogTerminal.tsx        # Термінал для відображення логів
│   └── ResultsTable.tsx       # Таблиця результатів аналізу
├── services/           # Бізнес-логіка та API
│   ├── api.ts                # REST API клієнт
│   ├── configuration.ts      # Управління конфігурацією
│   └── socket.ts            # WebSocket клієнт
├── data/              # Статичні дані
│   └── checkstyleRules.ts   # Опис правил Checkstyle
├── utils/             # Допоміжні функції
├── App.tsx            # Головний компонент
├── main.tsx          # Точка входу
└── index.css         # Глобальні стилі
```

### Потік даних

```
Користувач → AnalysisForm → API Service → Backend (POST /api/analyze)
                                          ↓
                              WebSocket ← Backend (real-time logs)
                                          ↓
                              LogTerminal (показ логів)
                                          ↓
                         Polling Status ← Backend (GET /api/status/{id})
                                          ↓ (кожні 2 сек до COMPLETED)
                         Fetch Results ← Backend (GET /api/results/{id})
                                          ↓
                              ResultsTable (відображення результатів)
```

---

## 🚀 Встановлення та запуск

### Передумови

Переконайтеся, що у вас установлено:

- **Node.js** версії 18.x або вище
- **npm** версії 9.x або вище
- **Backend-сервер** Checkstyle Hub (повинен працювати на `http://localhost:8000`)

### Крок 1: Клонування репозиторія

```bash
git clone https://github.com/your-username/react-checkstyle-hub.git
cd react-checkstyle-hub
```

### Крок 2: Встановлення залежностей

```bash
npm install
```

### Крок 3: Запуск у режимі розробки

```bash
npm run dev
```

Застосунок буде доступний за адресою: **http://localhost:5173** (або іншому порту, якщо 5173 зайнятий)

### Крок 4: Збірка для production

```bash
npm run build
```

Готова збірка буде у папці `dist/`

### Крок 5: Попередній перегляд production-збірки

```bash
npm run preview
```

---

## 📖 Використання

### 1. Запуск аналізу

1. Відкрийте застосунок у браузері
2. Уведіть URL публічного GitHub репозиторія (наприклад: `https://github.com/username/java-project`)
3. Натисніть кнопку **"Аналізувати"**
4. Спостерігайте за:
   - Логами в реальному часі (через WebSocket)
   - Статусом аналізу під формою (PENDING → CLONING → ANALYZING → COMPLETED)
5. Після завершення перегляньте результати в таблиці

### 2. Налаштування правил

1. Клацніть кнопку **"Налаштування"** у правому верхньому куті
2. У модальному вікні ви можете:
    - Увімкнути/вимкнути окремі правила
    - Використати кнопки **"Увімкнути всі"** або **"Вимкнути всі"**
    - Змінити рівень суворості (Ignore, Info, Warning, Error)
    - Налаштувати максимальну довжину рядка (80–200 символів)
3. Натисніть **"Зберегти"** для збереження змін
4. Використовуйте **"Скинути"** для скасовувати локальних змін

### 3. Перегляд результатів

У таблиці результатів відображаються:

- **Файл** — шлях до файлу з порушенням
- **Рядок** — номер рядка з проблемою
- **Рівень** — критичність (ERROR, WARNING, INFO)
- **Опис** — детальний опис порушення

---

## 📁 Структура проєкт

```
react-checkstyle-hub/
│
├── dist/                    # Production збірка (генерується)
├── node_modules/            # Залежності
├── public/                  # Статичні файли
├── src/
│   ├── components/          # React компоненти
│   │   ├── AnalysisForm.tsx
│   │   ├── ConfigurationModal.tsx
│   │   ├── LogTerminal.tsx
│   │   └── ResultsTable.tsx
│   │
│   ├── data/               # Статичні дані та константи
│   │   └── checkstyleRules.ts
│   │
│   ├── services/           # API та бізнес-логіка
│   │   ├── api.ts
│   │   ├── configuration.ts
│   │   └── socket.ts
│   │
│   ├── utils/              # Допоміжні функції
│   ├── App.tsx             # Головний компонент
│   ├── main.tsx            # Точка входу
│   └── index.css           # Глобальні стилі
│
├── eslint.config.js        # Конфігурація ESLint
├── postcss.config.ts       # Конфігурація PostCSS
├── tailwind.config.ts      # Конфігурація Tailwind CSS
├── tsconfig.json           # Конфігурація TypeScript
├── vite.config.ts          # Конфігурація Vite
├── package.json            # Залежності та скрипти
└── README.md              # Документація
```

---

## ⚙️ Конфігурація

### Налаштування Backend URL

За замовчуванням застосунок підключається до backend на `http://localhost:8000`.

Для зміни адреси відредагуйте файли:

**`src/services/api.ts`:**

```typescript
export const BACKEND_URL = 'http://your-backend-url:port';
```

**`src/services/configuration.ts`:**

```typescript
const API_BASE_URL = 'http://your-backend-url:port/api/checkstyle';
```

### Правила Checkstyle

Усі доступні правила описані у файлі `src/data/checkstyleRules.ts`. Ви можете:

- Додати нові правила
- Змінити опис, що існує
- Перекласти на інші мови
- Додати категорії

---

## 🔌 API

### REST Endpoints

#### 1. Запуск аналізу

```http
POST /api/analyze
Content-Type: application/json

{
  "repoUrl": "https://github.com/user/repo"
}

Response: "request-id-12345"
```

#### 2. Перевірка статусу

```http
GET /api/status/{requestId}

Response: {
  "id": 11,
  "status": "COMPLETED",
  "errorMessage": null,
  "createdAt": "2025-11-17T20:29:53.912808"
}
```

**Можливі статуси:**
- `PENDING` — Очікування початку аналізу
- `CLONING` — Клонування репозиторія
- `ANALYZING` — Аналіз Java файлів
- `COMPLETED` — Аналіз завершено успішно
- `FAILED` — Помилка під час аналізу

#### 3. Отримання результатів

```http
GET /api/results/{requestId}

Response: [
  {
    "id": 1,
    "filePath": "src/Main.java",
    "lineNumber": 42,
    "severity": "ERROR",
    "message": "Line is longer than 80 characters"
  }
]
```

**Важливо:** Результати доступні тільки після того, як статус стане `COMPLETED`.

#### 4. Отримання конфігурації

```http
GET /api/checkstyle/configuration

Response: {
  "id": 1,
  "configName": "default",
  "charset": "UTF-8",
  "severity": "error",
  "lineLength": 120,
  "avoidStarImport": true,
  ...
}
```

#### 5. Оновлення конфігурації

```http
PUT /api/checkstyle/configuration
Content-Type: application/json

{
  "severity": "warning",
  "lineLength": 100,
  "avoidStarImport": false
}

Response: {Updated configuration object}
```

### WebSocket

#### Підключення

```
Endpoint: ws://localhost:8000/ws-analyzer
Protocol: STOMP over SockJS
```

#### Підписка на логи

```
Topic: /topic/logs/{requestId}

Message format:
{
  "level": "INFO" | "WARNING" | "ERROR",
  "message": "Log message text"
}
```

---

## 🧪 Скрипти

| Команда               | Опис                                  |
|-----------------------|---------------------------------------|
| `npm run dev`         | Запуск dev-сервера з hot reload       |
| `npm run build`       | Production збірка                     |
| `npm run preview`     | Попередній перегляд production збірки |
| `npm run lint`        | Запуск ESLint                         |
| `npm run test`        | Запуск unit тестів                    |
| `npm run test:ui`     | Запуск тестів з UI інтерфейсом        |
| `npm run test:coverage` | Генерація звіту покриття тестами    |

---

## 🧪 Тестування

Проєкт покритий unit тестами для критичної бізнес-логіки.

### Структура тестів

```
src/
├── services/
│   ├── __tests__/
│   │   ├── api.test.ts        # Тести для API (startAnalysis, pollStatus, fetchResults)
│   │   └── socket.test.ts     # Тести для WebSocket (connectWebSocket, disconnectWebSocket)
├── components/
│   └── __tests__/
│       └── AnalysisForm.test.tsx  # Тести для форми аналізу
└── test/
    └── setup.ts               # Налаштування тестового середовища
```

### Що покрито тестами

#### ✅ API Service (`src/services/api.ts`)
- **startAnalysis()** — запуск аналізу
- **checkStatus()** — перевірка статусу
- **pollStatus()** — polling механізм (критична логіка!)
- **fetchResults()** — отримання результатів
- Повний інтеграційний тест workflow

#### ✅ WebSocket Service (`src/services/socket.ts`)
- **connectWebSocket()** — підключення до логів
- **disconnectWebSocket()** — відключення
- Обробка повідомлень (INFO, ERROR)
- Completion callbacks
- STOMP error handling

#### ✅ AnalysisForm Component (`src/components/AnalysisForm.tsx`)
- Рендеринг форми
- Валідація URL
- Обробка введення користувача
- Submit functionality
- Disabled states
- Accessibility

### Запуск тестів

```bash
# Запустити всі тести
npm run test

# Запустити тести з UI
npm run test:ui

# Генерація звіту покриття
npm run test:coverage
```

### Звіт покриття

Після запуску `npm run test:coverage` звіт буде доступний у папці `coverage/`:
- `coverage/index.html` — HTML звіт (відкрийте у браузері)
- `coverage/coverage-final.json` — JSON дані
- Консольний вивід з відсотками покриття

### Технології тестування

- **Vitest** — швидкий test runner (сумісний з Vite)
- **@testing-library/react** — тестування React компонентів
- **@testing-library/user-event** — симуляція дій користувача
- **jsdom** — браузерне середовище для Node.js

---

## 🎨 Стилізація

Проєкт використовує комбінацію:

- **Tailwind CSS** для utility-класів
- **Inline styles** для динамічних та компонентно-специфічних стилів
- **CSS animations** для плавних переходів

### Тема

- **Основний фон**: темний градієнт (від `rgb(2, 6, 23)` до `rgb(15, 23, 42)`)
- **Акцентні кольори**: синій (`rgb(59, 130, 246)`) та cyan (`rgb(6, 182, 212)`)
- **Текст**: білий з різними рівнями прозорості

---

## 🐛 Відомі проблеми

1. **WebSocket відключається** — переконайтеся, що backend запущено й доступний
2. **CORS помилки** — перевірте налаштування CORS на backend
3. **Повільний аналіз** — залежить від розміру репозиторія та продуктивності backend (30-120 секунд для середніх репозиторіїв)
4. **Результати не з'являються** — frontend автоматично чекає завершення аналізу через polling статусу

---

## 🤝 Внесок у проєкт

Якщо ви хочете покращити проєкт:

1. Створіть форк репозиторія
2. Створіть гілку для вашої функції: `git checkout -b feature/amazing-feature`
3. Закомітьте зміни: `git commit -m 'Add amazing feature'`
4. Запуште гілку: `git push origin feature/amazing-feature`
5. Відкрийте Pull Request

---

## 📝 Ліцензія

Цей проєкт розповсюджується під ліцензією MIT. Дивіться файл `LICENSE` для деталей.

---

## 👥 Автори

**CheckstyleHub Team**
